#include <stdio.h>
#include <stdbool.h>

int main(void){
	//BOOLEAN DATA TYPE
	//- Was introduced later and is not past of base so use #include <stdbool.h> to get booleans
	//Without special import, use 1 and 0 for true and false
	
	bool test = true;

	printf("%d",test);
	//prints 1 for true and 0 for false
 
	return 0;
}
